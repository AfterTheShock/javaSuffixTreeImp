package org.example;

import org.example.st.SuffixTree;

import java.util.List;

public class Application {
    public static void main(String[] args) {
        /*final String ANSI_RED = "\u001B[31m";
        final String ANSI_RESET = "\u001B[0m";

        SuffixTree suffixTree = new SuffixTree("AAAAATCTTTTCCCCGGACGG");

        List<String> aSequence = suffixTree.searchText("AAAA");
        List<String> tSequence = suffixTree.searchText("TTTT");
        List<String> cSequence = suffixTree.searchText("CCCC");
        List<String> gSequence = suffixTree.searchText("GGGG");

        System.out.println(ANSI_RED);
        aSequence.forEach(System.out::println);
        tSequence.forEach(System.out::println);
        cSequence.forEach(System.out::println);
        gSequence.forEach(System.out::println);
        System.out.println(ANSI_RESET);*/
    }
}